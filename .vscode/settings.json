// Place your settings in this file to overwrite default and user settings.
{
  "files.exclude": {
    "public/**/*.js": { "when": "$(basename).ts"},
    "public/**/*.map": true
  },
  "search.exclude": {
    "public/**/*.js": true,
    "public/**/*.map": true,
    "public/vendor/**/*.*": true
  }
}